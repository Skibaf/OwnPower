<nav class="navbar navbar-expand-lg navbar-dark bg-dark py-3">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"> <img src="../images/logo-no-background.svg" alt="logo" style= "width: 120px; height:40px" ></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Mis Clases</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="/ ">Mis Cursos</a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if current_user %>
           <ul class="nav-item"> <%= link_to "Editar profile", edit_user_registration_path, class: "btn btn-outline-warning" %></ul>
           <ul  class="nav-item"> <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-small btn-warning" %></ul>
       <% else %>
           <ul class="nav-but"> <%= link_to "Registrarse", new_user_registration_path, class: "btn btn-outline-warning"  %></ul>
           <ul class="nav-but"> <%= link_to "Sign In", new_user_session_path, class: "btn btn-small btn-warning"  %> </ul>
        <% end %>
        <% if current_user&.admin?  %>
          <ul class="nav-but"> <%= link_to "Admin Panel", about_path, class: "btn btn-small btn-info"  %></ul>
          <% end %>
      </ul>
    </div>
  </div>
</nav>